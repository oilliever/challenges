import { Logo } from '../components/Logo';
import { InfoIndex } from '../components/info';

export const Info = () => {
    return (
        <div className='container'>
            <Logo />
            <InfoIndex />
        </div>
    );
};
